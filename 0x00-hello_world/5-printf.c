#include <stdio.h>
/**
 * Description: main - program start from main function
 *
 * Return: always return 0 for successful execution
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

#include "main.h"
/**
 * print_diagonal - draws a diagonal line on the terminal.
 * @n: int type value
 * Return: void. no return.
 */

void print_diagonal(int n)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}

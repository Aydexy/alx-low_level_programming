#include "main.h"

/**
 * print_triangle - prints a triangle, followed by a new line.
 *
 * Return: 0 value on success
 */
int main(void)
{
    print_triangle(2);
    print_triangle(10);
    print_triangle(1);
    print_triangle(0);
    return (0);
}
